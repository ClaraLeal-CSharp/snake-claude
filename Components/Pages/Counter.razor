@* Counter.razor — removido @rendermode InteractiveServer *@
@page "/counter"

<PageTitle>Counter — Snake Claude</PageTitle>

<p role="status">Count: @_count</p>
<button @onclick="() => _count++">+1</button>

@code {
    private int _count = 0;
}
